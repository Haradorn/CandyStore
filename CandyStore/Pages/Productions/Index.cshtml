@page
@model CandyStore.Pages.Productions.IndexModel

@{
    ViewData["Title"] = "Productions";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Production[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Production[0].ProductType.ProductTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Production) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductType.ProductTypeName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProductionID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProductionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProductionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
